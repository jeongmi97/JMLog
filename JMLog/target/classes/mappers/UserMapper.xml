<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.dao.UserDAO">
	
	<select id="userChk" parameterType="String" resultType="UserVO">
		select * from user where email=#{email}
	</select>
	
	<insert id="join" parameterType="UserVO">
		insert into user(email, pw, nickname) values(#{email}, #{pw}, #{nickname})
	</insert>
	
	<select id="emailCheck" parameterType="String" resultType="int">
		select count(email) from user where email=#{email}
	</select>
	
	<select id="userBoardList" parameterType="String" resultType="BoardVO">
		select *
		from board
		where email=#{email}
		order by idx
	</select>
	
	<select id="listAll" parameterType="String" resultType="int">
		select count(idx) from board where email=#{email}
	</select>
</mapper>